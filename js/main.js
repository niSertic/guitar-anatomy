/*
 * guitar-anatomy — main JavaScript
 *
 * Responsibilities (populated across build steps):
 *   Step 3 — explode toggle & slider: read data-explode attrs, apply CSS transforms
 *   Step 4 — hover/focus data cards: show glassmorphic card with part info
 *   Step 5 — signal-path mode: highlight the audio chain in sequence
 */
